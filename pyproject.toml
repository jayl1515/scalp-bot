[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scalp-bot"
version = "0.1.0"
description = "Fixed-capital crypto scalping bot framework with profit sweep logic"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
